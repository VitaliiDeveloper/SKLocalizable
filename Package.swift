// swift-tools-version: 5.6
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let package = Package(
    name: "SKLocalizable",
    products: [
        .library(
            name: "SKLocalizable",
            targets: ["SKLocalizable"]),
    ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "SKLocalizable",
            dependencies: [])
    ]
)
